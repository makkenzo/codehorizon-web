'use client';

interface ProfileFormProps {}

const ProfileForm = ({}: ProfileFormProps) => {
    return (
        <div>
            <h1>ProfileForm</h1>
        </div>
    );
};

export default ProfileForm;
